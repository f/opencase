# Yedi Dakika assets

`lobby-cctv-still.png` was generated with OpenAI ImageGen for this fictional
case and visually inspected before its SHA-256 digest was authored in
`../case.yml`. Exact timing and identity clues remain in the structural report;
the image is supporting presentation, not a source of OCR-dependent logic.

Place additional authored local media here, then declare each file in
`../case.yml` with its MIME type, visibility and SHA-256 digest. Do not ship
placeholder media or put required solution facts only in pixels.
